geopandas==0.14.0  # Replace with the latest stable version if available
fiona==1.9.4      # Required for GeoPandas to handle spatial files
pyproj==3.6.0     # Coordinate reference system handling (GeoPandas dependency)
shapely==2.0.1    # Geometry operations (GeoPandas dependency)
